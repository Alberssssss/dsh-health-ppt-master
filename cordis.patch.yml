# One row owns the bundled skill provider and its soft router, so a profile
# override can enable or disable both together.
- insert:
    - id: health-ppt-master
      name: '@deepseek-ai/dsh-experimental-health-ppt-master'
